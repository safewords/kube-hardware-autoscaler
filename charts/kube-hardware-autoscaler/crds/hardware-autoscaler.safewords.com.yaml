# Generated by hack/gen-crds.sh from src/crd.rs - do not edit.
{
  "apiVersion": "apiextensions.k8s.io/v1",
  "kind": "CustomResourceDefinition",
  "metadata": {
    "name": "nodepowermanagementconfigs.hardware-autoscaler.safewords.com"
  },
  "spec": {
    "group": "hardware-autoscaler.safewords.com",
    "names": {
      "kind": "NodePowerManagementConfig",
      "plural": "nodepowermanagementconfigs",
      "shortNames": [
        "npmc"
      ],
      "singular": "nodepowermanagementconfig"
    },
    "scope": "Cluster",
    "versions": [
      {
        "additionalPrinterColumns": [
          {
            "jsonPath": ".status.pool",
            "name": "Pool",
            "type": "string"
          },
          {
            "jsonPath": ".spec.powerPolicy",
            "name": "Policy",
            "type": "string"
          },
          {
            "jsonPath": ".status.lastPowerAction.via",
            "name": "Via",
            "type": "string"
          },
          {
            "jsonPath": ".status.phase",
            "name": "Phase",
            "type": "string"
          },
          {
            "jsonPath": ".status.powerState",
            "name": "Power",
            "type": "string"
          },
          {
            "jsonPath": ".metadata.creationTimestamp",
            "name": "Age",
            "type": "date"
          }
        ],
        "name": "v1alpha1",
        "schema": {
          "openAPIV3Schema": {
            "description": "Auto-generated derived type for NodePowerManagementConfigSpec via `CustomResource`",
            "properties": {
              "spec": {
                "description": "A physical (or virtual) machine backing a Kubernetes node whose power state\ncan be controlled out-of-band.",
                "properties": {
                  "lifecycle": {
                    "default": {
                      "bootTimeoutSeconds": 900,
                      "drainTimeoutSeconds": 300,
                      "forceAfterDrainTimeout": false,
                      "shutdownTimeoutSeconds": 300
                    },
                    "description": "Timeouts and drain behaviour for power transitions.",
                    "properties": {
                      "bootTimeoutSeconds": {
                        "default": 900,
                        "description": "Seconds to wait for the node to become `Ready` after powering on.",
                        "format": "uint64",
                        "minimum": 0.0,
                        "type": "integer"
                      },
                      "drainTimeoutSeconds": {
                        "default": 300,
                        "description": "Seconds to wait for pods to be evicted before giving up.",
                        "format": "uint64",
                        "minimum": 0.0,
                        "type": "integer"
                      },
                      "forceAfterDrainTimeout": {
                        "default": false,
                        "description": "Power off anyway when the drain times out. When false (default) the\nscale-down is aborted and the node is uncordoned.",
                        "type": "boolean"
                      },
                      "shutdownTimeoutSeconds": {
                        "default": 300,
                        "description": "Seconds to wait for a graceful (ACPI) shutdown before forcing power off.",
                        "format": "uint64",
                        "minimum": 0.0,
                        "type": "integer"
                      }
                    },
                    "type": "object"
                  },
                  "nodeName": {
                    "description": "Name of the Kubernetes `Node` object this machine registers as. Must\nequal `metadata.name`. Pool membership follows from this Node's labels\n(see `NodeScalingPool.spec.nodeSelector`).",
                    "type": "string"
                  },
                  "powerInterfaces": {
                    "description": "Management interfaces, in priority order. Every operation tries them in\nturn (each bounded by its own timeout) and the first success wins, so\ne.g. IPMI can be backed by Wake-on-LAN for powering on.",
                    "items": {
                      "description": "Out-of-band management interface configuration.\n\n`driver` selects an entry from the driver catalog (`kube-hardware-autoscaler drivers`\nlists them together with the schema of their `config`). Credentials are\nalways read from a `Secret` in the operator's namespace.",
                      "properties": {
                        "actions": {
                          "description": "Operations this interface is used for (`status`, `powerOn`, `powerOff`).\nAll of them when omitted.",
                          "items": {
                            "description": "An operation a power interface can be used for.",
                            "enum": [
                              "status",
                              "powerOn",
                              "powerOff"
                            ],
                            "type": "string"
                          },
                          "nullable": true,
                          "type": "array"
                        },
                        "config": {
                          "default": {},
                          "description": "Driver-specific configuration; validated against the driver's schema.",
                          "type": "object",
                          "x-kubernetes-preserve-unknown-fields": true
                        },
                        "credentialsSecretRef": {
                          "description": "Secret holding the interface credentials (for drivers that need them).",
                          "nullable": true,
                          "properties": {
                            "name": {
                              "description": "Secret name.",
                              "type": "string"
                            },
                            "passwordKey": {
                              "default": "password",
                              "description": "Key holding the password. Defaults to `password`.",
                              "type": "string"
                            },
                            "usernameKey": {
                              "default": "username",
                              "description": "Key holding the user name. Defaults to `username`.",
                              "type": "string"
                            }
                          },
                          "required": [
                            "name"
                          ],
                          "type": "object"
                        },
                        "driver": {
                          "description": "Driver name, e.g. `ipmi`, `redfish`, `pikvm` or `wakeOnLan`.",
                          "type": "string"
                        },
                        "name": {
                          "description": "Optional label used in status, events and logs. Defaults to the driver name.",
                          "nullable": true,
                          "type": "string"
                        },
                        "timeoutSeconds": {
                          "description": "Upper bound for one operation on this interface before falling back to\nthe next one. Defaults to the operator-wide timeout.",
                          "format": "uint64",
                          "minimum": 0.0,
                          "nullable": true,
                          "type": "integer"
                        }
                      },
                      "required": [
                        "driver"
                      ],
                      "type": "object"
                    },
                    "minItems": 1,
                    "type": "array"
                  },
                  "powerPolicy": {
                    "default": "Auto",
                    "description": "How the machine's power state is decided.",
                    "enum": [
                      "Auto",
                      "AlwaysOn",
                      "AlwaysOff"
                    ],
                    "type": "string"
                  }
                },
                "required": [
                  "nodeName",
                  "powerInterfaces"
                ],
                "type": "object"
              },
              "status": {
                "nullable": true,
                "properties": {
                  "allocatable": {
                    "description": "Last-known allocatable resources of the node (kept while it is off).",
                    "nullable": true,
                    "properties": {
                      "cpuMillis": {
                        "format": "int64",
                        "type": "integer"
                      },
                      "memoryBytes": {
                        "format": "int64",
                        "type": "integer"
                      },
                      "pods": {
                        "format": "int64",
                        "type": "integer"
                      }
                    },
                    "required": [
                      "cpuMillis",
                      "memoryBytes",
                      "pods"
                    ],
                    "type": "object"
                  },
                  "conditions": {
                    "description": "Safety checks gating power actions: `NodeFound`, `PoolMembership`,\n`IdentityVerified`, `PowerStateConsistent`.",
                    "items": {
                      "description": "A status condition (the usual Kubernetes shape).",
                      "properties": {
                        "lastTransitionTime": {
                          "format": "date-time",
                          "type": "string"
                        },
                        "message": {
                          "type": "string"
                        },
                        "reason": {
                          "type": "string"
                        },
                        "status": {
                          "description": "`True`, `False` or `Unknown`.",
                          "type": "string"
                        },
                        "type": {
                          "type": "string"
                        }
                      },
                      "required": [
                        "lastTransitionTime",
                        "reason",
                        "status",
                        "type"
                      ],
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "drainFailedAt": {
                    "description": "When the last drain failed; the autoscaler backs off from this node for a while.",
                    "format": "date-time",
                    "nullable": true,
                    "type": "string"
                  },
                  "forcedOffAt": {
                    "description": "When a forced (hard) power off was issued during the current `PoweringOff` phase.",
                    "format": "date-time",
                    "nullable": true,
                    "type": "string"
                  },
                  "interfaceWarnings": {
                    "description": "Interfaces that are misconfigured or failed before a lower-priority\none succeeded during the last status check, as \"name: error\".",
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  "lastPowerAction": {
                    "description": "The last power action issued to the management interface.",
                    "nullable": true,
                    "properties": {
                      "action": {
                        "description": "`PowerOn`, `PowerOff` or `ForceOff`.",
                        "type": "string"
                      },
                      "succeeded": {
                        "description": "Whether a management interface accepted the request.",
                        "type": "boolean"
                      },
                      "time": {
                        "format": "date-time",
                        "type": "string"
                      },
                      "via": {
                        "description": "The interface that carried the action out (or the last one tried).",
                        "nullable": true,
                        "type": "string"
                      }
                    },
                    "required": [
                      "action",
                      "succeeded",
                      "time"
                    ],
                    "type": "object"
                  },
                  "message": {
                    "description": "Human-readable details about the current state or the last error.",
                    "nullable": true,
                    "type": "string"
                  },
                  "nodeLabels": {
                    "additionalProperties": {
                      "type": "string"
                    },
                    "description": "Last-known labels of the node (kept while it is off).",
                    "nullable": true,
                    "type": "object"
                  },
                  "nodeReady": {
                    "default": false,
                    "description": "Whether the Kubernetes node currently reports `Ready`.",
                    "type": "boolean"
                  },
                  "phase": {
                    "default": "Unknown",
                    "description": "Lifecycle phase of a managed machine.",
                    "enum": [
                      "Unknown",
                      "PoweringOn",
                      "On",
                      "Draining",
                      "PoweringOff",
                      "Off",
                      "Error"
                    ],
                    "type": "string"
                  },
                  "phaseSince": {
                    "description": "When the current phase was entered.",
                    "format": "date-time",
                    "nullable": true,
                    "type": "string"
                  },
                  "pool": {
                    "description": "The `NodeScalingPool` whose `nodeSelector` matches this machine's Node, if\nexactly one does.",
                    "nullable": true,
                    "type": "string"
                  },
                  "powerState": {
                    "default": "Unknown",
                    "description": "Observed power state as reported by the management interface.",
                    "enum": [
                      "On",
                      "Off",
                      "Unknown"
                    ],
                    "type": "string"
                  },
                  "scalingDecision": {
                    "description": "Latest decision from the pool autoscaler (only honoured with\n`powerPolicy: Auto`, and only while the machine is still a member of\nthe pool that made it).",
                    "nullable": true,
                    "properties": {
                      "pool": {
                        "default": "",
                        "description": "The `NodeScalingPool` that made the decision. A decision is ignored (and\ncleared) once the machine is no longer a member of that pool, e.g.\nbecause the pool was deleted or the Node's labels changed.",
                        "type": "string"
                      },
                      "reason": {
                        "type": "string"
                      },
                      "target": {
                        "description": "Target power state.",
                        "enum": [
                          "On",
                          "Off"
                        ],
                        "type": "string"
                      },
                      "time": {
                        "format": "date-time",
                        "type": "string"
                      }
                    },
                    "required": [
                      "reason",
                      "target",
                      "time"
                    ],
                    "type": "object"
                  }
                },
                "type": "object"
              }
            },
            "required": [
              "spec"
            ],
            "title": "NodePowerManagementConfig",
            "type": "object",
            "x-kubernetes-validations": [
              {
                "message": "metadata.name must equal spec.nodeName",
                "rule": "self.metadata.name == self.spec.nodeName"
              }
            ]
          }
        },
        "served": true,
        "storage": true,
        "subresources": {
          "status": {}
        }
      }
    ]
  }
}
---
{
  "apiVersion": "apiextensions.k8s.io/v1",
  "kind": "CustomResourceDefinition",
  "metadata": {
    "name": "nodescalingpools.hardware-autoscaler.safewords.com"
  },
  "spec": {
    "group": "hardware-autoscaler.safewords.com",
    "names": {
      "kind": "NodeScalingPool",
      "plural": "nodescalingpools",
      "shortNames": [
        "nspool"
      ],
      "singular": "nodescalingpool"
    },
    "scope": "Cluster",
    "versions": [
      {
        "additionalPrinterColumns": [
          {
            "jsonPath": ".spec.minOnline",
            "name": "Min",
            "type": "integer"
          },
          {
            "jsonPath": ".spec.maxOnline",
            "name": "Max",
            "type": "integer"
          },
          {
            "jsonPath": ".status.onlineNodes",
            "name": "Online",
            "type": "integer"
          },
          {
            "jsonPath": ".status.totalNodes",
            "name": "Total",
            "type": "integer"
          },
          {
            "jsonPath": ".status.pendingPods",
            "name": "Pending",
            "type": "integer"
          },
          {
            "jsonPath": ".metadata.creationTimestamp",
            "name": "Age",
            "type": "date"
          }
        ],
        "name": "v1alpha1",
        "schema": {
          "openAPIV3Schema": {
            "description": "Auto-generated derived type for NodeScalingPoolSpec via `CustomResource`",
            "properties": {
              "spec": {
                "description": "A class of machines, selected by the labels of their Kubernetes Nodes, that\nare powered on and off together based on demand. Every `NodePowerManagementConfig` whose\nNode matches `nodeSelector` is a member; a Node matched by more than one\npool is a conflict and belongs to none.",
                "properties": {
                  "maxOnline": {
                    "description": "Maximum number of machines powered on. Unlimited when omitted.",
                    "format": "uint32",
                    "minimum": 0.0,
                    "nullable": true,
                    "type": "integer"
                  },
                  "minOnline": {
                    "default": 0,
                    "description": "Minimum number of machines kept powered on.",
                    "format": "uint32",
                    "minimum": 0.0,
                    "type": "integer"
                  },
                  "nodeSelector": {
                    "description": "Which Nodes belong to this pool, by label (the \"node type\"). Required\nand non-empty; only Nodes that also have a `NodePowerManagementConfig` are powered.",
                    "properties": {
                      "matchExpressions": {
                        "items": {
                          "properties": {
                            "key": {
                              "type": "string"
                            },
                            "operator": {
                              "enum": [
                                "In",
                                "NotIn",
                                "Exists",
                                "DoesNotExist"
                              ],
                              "type": "string"
                            },
                            "values": {
                              "items": {
                                "type": "string"
                              },
                              "type": "array"
                            }
                          },
                          "required": [
                            "key",
                            "operator"
                          ],
                          "type": "object"
                        },
                        "type": "array"
                      },
                      "matchLabels": {
                        "additionalProperties": {
                          "type": "string"
                        },
                        "type": "object"
                      }
                    },
                    "type": "object"
                  },
                  "scaleDown": {
                    "default": {
                      "delayAfterScaleUpSeconds": 600,
                      "drainFailureBackoffSeconds": 1800,
                      "enabled": true,
                      "maxNodesPerStep": 1,
                      "unneededSeconds": 600,
                      "utilizationThresholdPercent": 50
                    },
                    "properties": {
                      "delayAfterScaleUpSeconds": {
                        "default": 600,
                        "description": "Do not scale down within this many seconds after a scale up.",
                        "format": "uint64",
                        "minimum": 0.0,
                        "type": "integer"
                      },
                      "drainFailureBackoffSeconds": {
                        "default": 1800,
                        "description": "Do not retry a node whose drain failed within this many seconds.",
                        "format": "uint64",
                        "minimum": 0.0,
                        "type": "integer"
                      },
                      "enabled": {
                        "default": true,
                        "type": "boolean"
                      },
                      "maxNodesPerStep": {
                        "default": 1,
                        "description": "Maximum machines concurrently being scaled down.",
                        "format": "uint32",
                        "minimum": 0.0,
                        "type": "integer"
                      },
                      "unneededSeconds": {
                        "default": 600,
                        "description": "How long a node must be continuously unneeded before it is powered off.",
                        "format": "uint64",
                        "minimum": 0.0,
                        "type": "integer"
                      },
                      "utilizationThresholdPercent": {
                        "default": 50,
                        "description": "A node is considered unneeded when both its CPU and memory requests are\nbelow this percentage of allocatable.",
                        "format": "uint32",
                        "minimum": 0.0,
                        "type": "integer"
                      }
                    },
                    "type": "object"
                  },
                  "scaleUp": {
                    "default": {
                      "enabled": true,
                      "maxNodesPerStep": 3,
                      "pendingPodGraceSeconds": 30
                    },
                    "properties": {
                      "enabled": {
                        "default": true,
                        "type": "boolean"
                      },
                      "maxNodesPerStep": {
                        "default": 3,
                        "description": "Maximum machines powered on per autoscaler cycle.",
                        "format": "uint32",
                        "minimum": 0.0,
                        "type": "integer"
                      },
                      "pendingPodGraceSeconds": {
                        "default": 30,
                        "description": "Unschedulable pods younger than this are ignored (lets the scheduler settle).",
                        "format": "uint64",
                        "minimum": 0.0,
                        "type": "integer"
                      }
                    },
                    "type": "object"
                  }
                },
                "required": [
                  "nodeSelector"
                ],
                "type": "object"
              },
              "status": {
                "nullable": true,
                "properties": {
                  "conflicts": {
                    "description": "NodePowerManagementConfigs whose Node also matches another pool; excluded from both.",
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  "lastScaleDownTime": {
                    "format": "date-time",
                    "nullable": true,
                    "type": "string"
                  },
                  "lastScaleUpTime": {
                    "format": "date-time",
                    "nullable": true,
                    "type": "string"
                  },
                  "members": {
                    "description": "NodePowerManagementConfigs currently in this pool.",
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  "message": {
                    "nullable": true,
                    "type": "string"
                  },
                  "onlineNodes": {
                    "default": 0,
                    "description": "Machines that are on or powering on.",
                    "format": "uint32",
                    "minimum": 0.0,
                    "type": "integer"
                  },
                  "pendingPods": {
                    "default": 0,
                    "description": "Unschedulable pods this pool could serve.",
                    "format": "uint32",
                    "minimum": 0.0,
                    "type": "integer"
                  },
                  "totalNodes": {
                    "default": 0,
                    "format": "uint32",
                    "minimum": 0.0,
                    "type": "integer"
                  },
                  "unneededSince": {
                    "additionalProperties": {
                      "format": "date-time",
                      "type": "string"
                    },
                    "description": "Since when each member (by NodePowerManagementConfig name) has been continuously unneeded.",
                    "type": "object"
                  }
                },
                "type": "object"
              }
            },
            "required": [
              "spec"
            ],
            "title": "NodeScalingPool",
            "type": "object",
            "x-kubernetes-validations": [
              {
                "message": "spec.nodeSelector must not be empty",
                "rule": "(has(self.spec.nodeSelector.matchLabels) && size(self.spec.nodeSelector.matchLabels) > 0) || (has(self.spec.nodeSelector.matchExpressions) && size(self.spec.nodeSelector.matchExpressions) > 0)"
              }
            ]
          }
        },
        "served": true,
        "storage": true,
        "subresources": {
          "status": {}
        }
      }
    ]
  }
}
